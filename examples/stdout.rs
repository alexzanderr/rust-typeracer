use std::io::Write;
use std::io::{
    stdout,
    Stdout
};
fn main() {
    let mut stdout = stdout();
    // stdout.write_fmt(format_args!("{}", "some text")).unwrap();
    // write!(&mut stdout, "{}", "some text").unwrap();
}
